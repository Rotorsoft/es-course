# Required: secret for signing session tokens (random default in dev)
SESSION_SECRET=change-me-in-production

# Optional: enable Google SSO (leave blank to use only basic auth)
# 1. Go to https://console.cloud.google.com/ → Create/select project
# 2. APIs & Services → Credentials → Create OAuth 2.0 Client ID (Web application)
# 3. Authorized JavaScript origins: http://localhost:5173
# 4. Copy Client ID below
# 5. Configure OAuth consent screen (External, scopes: openid, email, profile)
GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_ID=
